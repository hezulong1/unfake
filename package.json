{
  "name": "unfake-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build --filter unfake",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "conventional-changelog-cli": "catalog:dev",
    "eslint": "catalog:dev",
    "eslint-config-h21": "catalog:dev",
    "eslint-plugin-lit": "catalog:dev",
    "minimist": "catalog:dev",
    "tsdown": "catalog:dev",
    "turbo": "catalog:dev",
    "typescript": "catalog:dev"
  }
}
